This folder contains the example scripts about Object Tracking.

In order to execute the code:

1. Download this github repo
2. Install Anaconda (https://www.anaconda.com/download)
3. Open the Anaconda Prompt (on Windows) or a terminal (on macOS/Linux).
4. Run these lines 
    - conda create --name nome_env python=3.8  # Creazione di un nuovo enviroment con python 3.8
    - conda activate nome_env
    - conda install conda-forge::opencv
    - conda install spyder (OPTIONAL: Spyder is a IDE, but you can use the one you prefer)
    - spyder (OPTIONAL: to run Spyder)

Open the scripts in the IDE and execute!

You can find the two pre-trained models of the MobileNet-SSD framework, here:
https://github.com/PINTO0309/MobileNet-SSD-RealSense/tree/master
